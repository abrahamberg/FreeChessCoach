# FreeChessCoach — Implementation Plan

**There is no open plan.**

The last plan (import limits, stat archive, guided import; Phases 67–72) is fully shipped and described in `docs/architecture.md`.

When new work needs a plan, create one with:
- A source-spec pointer
- An "already exists (verified in code)" section  
- Layering reminder
- `## Phase N` / `### Task N.M` blocks with **Read:** line, **Files:**, unchecked `- [ ]` TDD steps and a `Commit:` line

Continue phase numbering from 73.