apiVersion: v1
kind: Service
metadata:
  name: {{ include "freechesscoach.componentName" (dict "ctx" . "component" "api") }}
  labels:
    {{- include "freechesscoach.labels" (dict "ctx" . "component" "api") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
  selector:
    {{- include "freechesscoach.selectorLabels" (dict "ctx" . "component" "api") | nindent 4 }}
