apiVersion: v1
kind: Service
metadata:
  name: {{ include "freechesscoach.componentName" (dict "ctx" . "component" "engine") }}
  labels:
    {{- include "freechesscoach.labels" (dict "ctx" . "component" "engine") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.engine.service.port }}
      targetPort: http
  selector:
    {{- include "freechesscoach.selectorLabels" (dict "ctx" . "component" "engine") | nindent 4 }}
