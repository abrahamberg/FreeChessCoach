apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "freechesscoach.componentName" (dict "ctx" . "component" "api") }}
  labels:
    {{- include "freechesscoach.labels" (dict "ctx" . "component" "api") | nindent 4 }}
spec:
  {{- if not .Values.api.autoscaling.enabled }}
  replicas: {{ .Values.api.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "freechesscoach.selectorLabels" (dict "ctx" . "component" "api") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "freechesscoach.selectorLabels" (dict "ctx" . "component" "api") | nindent 8 }}
    spec:
      {{- include "freechesscoach.imagePullSecrets" . | nindent 6 }}
      securityContext:
        {{- include "freechesscoach.podSecurityContext" . | nindent 8 }}
      containers:
        - name: api
          image: {{ include "freechesscoach.image" (dict "ctx" . "image" .Values.api.image) | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["node", "dist-bundle/server.mjs"]
          securityContext:
            {{- include "freechesscoach.containerSecurityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.api.containerPort }}
          env:
            - name: PORT
              value: {{ .Values.api.containerPort | quote }}
            {{- include "freechesscoach.env.database" . | nindent 12 }}
            {{- include "freechesscoach.env.app" . | nindent 12 }}
          # architecture §11: readiness = /readyz (checks DB); /healthz is liveness only.
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
      {{- with .Values.api.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.api.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
