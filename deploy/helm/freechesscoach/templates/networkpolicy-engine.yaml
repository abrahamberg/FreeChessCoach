{{- if .Values.networkPolicy.enabled }}
{{/* architecture §11: "engine accepts only api+worker". */}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "freechesscoach.componentName" (dict "ctx" . "component" "engine") }}
  labels:
    {{- include "freechesscoach.labels" (dict "ctx" . "component" "engine") | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "freechesscoach.selectorLabels" (dict "ctx" . "component" "engine") | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              {{- include "freechesscoach.selectorLabels" (dict "ctx" . "component" "api") | nindent 14 }}
        - podSelector:
            matchLabels:
              {{- include "freechesscoach.selectorLabels" (dict "ctx" . "component" "worker") | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.engine.containerPort }}
{{- end }}
